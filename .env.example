# Luxembourg Backend Configuration

# Server settings
HOST=0.0.0.0
PORT=8000

# Session management
SESSION_TTL=3600          # Session TTL in seconds (default: 1 hour)
CLEANUP_INTERVAL=300      # Cleanup interval in seconds (default: 5 minutes)
POLL_TIMEOUT=30           # Plugin polling timeout in seconds

# Agent settings
MAX_JSON_RETRIES=3        # Max retries for JSON parsing failures

# OpenRouter API Key (optional - users provide their own via plugin)
# Only set this if you want to use a server-side default key (not recommended for production)
# OPENROUTER_KEY=your-openrouter-api-key-here
